# Generated mobile rendering assets

`ZunoMobileRenderer.asset` and `ZunoMobilePipeline.asset` are created here by
`ZunoProjectSetup` on first import with Unity 6000.3.20f1. Commit those generated
assets and their `.meta` files after the first verified Unity import so their
serialization matches the pinned Editor exactly.
